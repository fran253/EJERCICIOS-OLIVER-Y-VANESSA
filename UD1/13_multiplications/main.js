function printMultiplicationTables() {

    console.log(cadena)
    for (x = 1; x < 11; x++) {
        let cadena = `${x}:`
        for(y = 1; y <11; y++){
            cadena += `${i* j}\t`
            
        };
        
        console.log(cadena)
        
    }
}

printMultiplicationTables()